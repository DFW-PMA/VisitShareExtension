# CLAUDE.md — VisitShareExtension
# Developer: Daryl Cox — JustMacApps

See `~/.claude/CLAUDE.md` for shared conventions, coding standards, and reusable engineering
patterns (Hungarian prefixes, `@JmEntityInfo` macro, versioning format, `appLogMsg()`-only
logging, CHICKEN-TRACKS, no-deletion policy, backup convention, the Swift 6 fix-pattern catalog,
persistence architecture, macOS `.sheet()`/`openWindow()` convention, AdMob/ScrollView pattern,
etc.) — that file now covers everything that used to be copy-pasted into every project's own
`CLAUDE.md`.

**Consolidated 2026-09-11:** this file previously held a stale, unrelated copy of the shared
cross-project narrative (a side effect of the old copy-everywhere workflow) — none of that content
was actually about VisitShareExtension. The original is backed up at
`_ClaudeMD_Consolidation_Backups_2026-09-11/Dfw-PTS_OSx_apps/VisitShareExtension/VisitShareExtension/CLAUDE.md`
if anything from it is ever needed.

---

## VisitShareExtension's own history

Nothing recorded here yet as of 2026-09-11. Add this project's own dated notes below as real work
happens on it, rather than re-copying the global file's content into this one.
